#!/bin/bash
# Print the full Cloudflare Tunnel configuration JSON.
set -e
curl -4 -fsS -H "Authorization: Bearer $CF_API_TOKEN" \
    "$CF_API/accounts/$CF_ACCOUNT_ID/cfd_tunnel/$CF_TUNNEL_ID/configurations" \
    | jq '.result'
